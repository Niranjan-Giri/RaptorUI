import os
from dotenv import load_dotenv

# Load environment variables from .env file (if present)
load_dotenv()

# No server-side AI service is used in this build. Keep this file for future configuration.
